export default function RelevamientosPage() {
  return (
    <div className="mx-auto max-w-2xl rounded-lg border border-dashed border-base-border p-8 text-center">
      <h1 className="font-display text-xl font-semibold text-navy-900">Relevamientos</h1>
      <p className="mt-2 text-sm text-navy-700">
        Pegar múltiples códigos y generar una planilla exportable (Excel/PDF) para auditorías
        físicas. Se construye en el próximo paso.
      </p>
    </div>
  );
}
